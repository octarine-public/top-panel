import { Player } from "github.com/octarine-public/wrapper/index"

export class GUIPlayer {
	constructor(private readonly player: Player) {}

	public UpdateGUI(skipBottomData?: boolean) {
		/** */
	}
}
